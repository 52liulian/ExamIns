# 考试指令播放系统 - 设计文档

## 一、系统架构

### 1.1 架构概述

采用 **分层架构** 设计，分为展示层、业务逻辑层和数据层。

```
┌─────────────────────────────────────────────────────────────┐
│                    展示层 (Presentation)                     │
│  MainDialog ── GaokaoDialog ── PutongDialog ── ExamInfo    │
│  ShowInfo                                                   │
├─────────────────────────────────────────────────────────────┤
│                  业务逻辑层 (Business Logic)                  │
│  AudioPlayer ── SoundData ── NTPSync ── SettingsManager     │
├─────────────────────────────────────────────────────────────┤
│                      数据层 (Data)                           │
│  config/settings.json ── config/ntp_config.json ── Mp3/    │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 模块关系图

```
main.py (入口)
    │
    ├── dialog.py (主选择界面)
    │       ├── gaokao.py (高考模式)
    │       └── putong.py (普通模式)
    │                   │
    │                   └── exam_info.py (考试信息界面)
    │                               ├── show_info.py (指令列表)
    │                               ├── data.py (指令数据)
    │                               └── player.py (音频播放)
    │
    ├── settings.py (配置管理)
    └── ntp_sync.py (网络校时)
```

## 二、UI设计

### 2.1 界面设计原则

| 原则 | 说明 |
|------|------|
| 简洁直观 | 减少操作步骤，一目了然 |
| 响应迅速 | 界面操作即时反馈 |
| 信息清晰 | 关键信息突出显示 |
| 布局合理 | 符合用户操作习惯 |

### 2.2 界面原型

#### 2.2.1 主选择界面

```
┌─────────────────────────────────┐
│    考试指令播放系统 v1.0.1      │
├─────────────────────────────────┤
│                                 │
│    ○ 高考模式      ○ 普通模式   │
│                                 │
│    □ 启动时自动网络校时          │
│                                 │
│           [进入考试]            │
│                                 │
└─────────────────────────────────┘
```

#### 2.2.2 考试模式选择界面

```
┌─────────────────────────────────┐
│    考试指令播放系统              │
├─────────────────────────────────┤
│                                 │
│    请选择考试科目：[语文▼]      │
│                                 │
│    [返回]        [确定]         │
│                                 │
└─────────────────────────────────┘
```

#### 2.2.3 考试信息界面

```
┌─────────────────────────────────────────────────┐
│ 科目: 语文         开始时间: 09:00:00            │
│                   结束时间: 11:30:00            │
├─────────────────────────────────────────────────┤
│ 系统时间: [14:30:25]                            │
├─────────────────────────────────────────────────┤
│ 当前/下条指令: 即将播放指令时间：09:00:00        │
├─────────────────────────────────────────────────┤
│ 指令内容:                                       │
│ ┌───────────────────────────────────────────┐   │
│ │ (铃声，长15秒)现在开始答题，现在开始答题。 │   │
│ └───────────────────────────────────────────┘   │
├─────────────────────────────────────────────────┤
│              [查看指令列表]  [停止]             │
└─────────────────────────────────────────────────┘
```

### 2.3 颜色方案

| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | #FFFFFF | 白色背景 |
| 标题 | #333333 | 深灰色标题 |
| 按钮 | #4A90D9 | 蓝色按钮 |
| 按钮悬停 | #357ABD | 深蓝色悬停 |
| 重要信息 | #D9534F | 红色强调 |

## 三、数据模型

### 3.1 指令数据模型

```python
@dataclass
class Sound:
    play_time: int        # 播放时间（分钟）
    play_ins: str         # 指令内容
    play_file: str        # 音频文件名
    is_before_exam: bool  # 是否为考前指令
```

### 3.2 考试时间配置

#### 高考模式

| 科目 | 开始时间 | 结束时间 |
|------|---------|---------|
| 语文 | 09:00 | 11:30 |
| 数学 | 15:00 | 17:00 |
| 外语 | 15:00 | 17:00 |
| 物理/历史 | 09:00 | 10:15 |
| 化学 | 08:30 | 09:45 |
| 思想政治 | 14:30 | 15:45 |
| 地理 | 11:00 | 12:15 |
| 生物学 | 17:00 | 18:15 |

#### 普通模式

| 科目 | 开始时间 | 结束时间 |
|------|---------|---------|
| 语文 | 09:00 | 11:30 |
| 数学 | 14:30 | 16:30 |
| 历史 | 16:50 | 18:05 |
| 外语 | 08:00 | 10:00 |
| 化学 | 10:20 | 11:35 |
| 物理 | 14:30 | 15:45 |
| 生物 | 16:05 | 17:20 |
| 政治 | 08:00 | 09:15 |
| 地理 | 09:35 | 10:50 |
| 物理/历史 | 10:30 | 11:45 |
| 化学/政治 | 14:30 | 15:45 |
| 生物/地理 | 15:55 | 17:10 |
| 其他 | 自定义 | 自定义 |

### 3.3 配置文件结构

#### settings.json

```json
{
    "auto_ntp_sync": true
}
```

#### ntp_config.json

```json
{
    "ntp_servers": [
        "ntp.aliyun.com",
        "time.windows.com",
        "time.apple.com"
    ]
}
```

## 四、核心流程

### 4.1 启动流程

```
启动程序
    │
    ├── 初始化日志
    │
    ├── 读取配置
    │       └── settings.json
    │
    ├── 网络校时（可选）
    │       └── ntp_sync.py
    │
    └── 显示主界面
            └── MainDialog
```

### 4.2 考试流程

```
选择考试模式
    │
    ├── 高考模式 → 选择科目 → 进入考试
    │
    └── 普通模式 → 选择科目 → 设置时间 → 进入考试
                                    │
                                    ├── 初始化指令列表
                                    │       └── SoundData
                                    │
                                    ├── 启动计时器
                                    │       └── QTimer
                                    │
                                    ├── 时间到达 → 播放指令
                                    │       └── AudioPlayer
                                    │
                                    └── 考试结束 → 返回选择界面
```

### 4.3 指令播放流程

```
时间触发
    │
    ├── 查找对应指令
    │       └── SoundData.sound_data
    │
    ├── 检查音频文件
    │       └── os.path.exists(file_path)
    │
    ├── 播放音频
    │       └── pygame.mixer.music.play()
    │
    ├── 显示指令内容
    │       └── ins_content_label.setText()
    │
    └── 播放完成 → 显示下条指令
```

## 五、技术决策

### 5.1 技术选型

| 技术 | 选型 | 理由 |
|------|------|------|
| 语言 | Python | 简洁易读，生态丰富 |
| GUI框架 | PyQt5 | 功能强大，跨平台 |
| 音频播放 | pygame | 轻量级，易于集成 |
| 配置管理 | JSON | 简单易读，便于修改 |
| 打包工具 | Nuitka | 生成独立可执行文件 |

### 5.2 关键设计决策

| 决策 | 说明 |
|------|------|
| 时间驱动 | 使用系统时间作为触发器，确保指令按时播放 |
| 音频预加载 | 音频文件在播放前检查，避免播放失败 |
| 配置持久化 | 配置文件存储在config目录，便于备份和迁移 |
| 日志记录 | 完整的日志记录，便于问题排查 |

### 5.3 扩展性设计

| 扩展点 | 设计考虑 |
|------|---------|
| 新增考试模式 | 通过继承ExamInfo类实现 |
| 新增指令 | 在SoundData中添加新的Sound对象 |
| 新增音频格式 | 在AudioPlayer中扩展格式支持 |
| 多语言支持 | 将指令内容提取为配置文件 |
