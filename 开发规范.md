# 考试指令播放系统 - 开发规范

## 一、代码规范

### 1.1 Python代码规范

#### 1.1.1 PEP8规范

- 遵循 PEP8 代码风格指南
- 使用 4 空格缩进，不使用制表符
- 行长度不超过 120 个字符
- 文件名使用小写字母，单词间用下划线分隔（如 `exam_info.py`）

#### 1.1.2 命名规范

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| 模块名 | 小写字母，下划线分隔 | `exam_info.py` |
| 类名 | 驼峰命名法 | `MainDialog` |
| 方法名 | 小写字母，下划线分隔 | `init_ui()` |
| 变量名 | 小写字母，下划线分隔 | `start_time` |
| 常量名 | 大写字母，下划线分隔 | `VERSION` |
| 私有成员 | 下划线开头 | `_set_gaokao_sound()` |

#### 1.1.3 注释规范

- **模块级注释**：文件开头说明模块功能
- **类级注释**：类定义后说明类的作用
- **方法级注释**：每个方法必须包含功能描述、参数说明、返回值说明

```python
def play_ins(self, index):
    """播放指定索引的考试指令
    
    Args:
        index (int): 指令索引
        
    Returns:
        None
    """
```

#### 1.1.4 导入规范

- 导入顺序：标准库 → 第三方库 → 本地模块
- 每个模块分组用空行分隔

```python
import sys
import os
import logging
from datetime import datetime

from PyQt5.QtWidgets import QDialog, QVBoxLayout

from data import SoundData
from player import AudioPlayer
```

### 1.2 PyQt5规范

#### 1.2.1 UI开发规范

- 使用 Qt 的信号槽机制进行事件处理
- 避免在主线程中执行耗时操作
- UI 初始化统一在 `init_ui()` 方法中完成

#### 1.2.2 资源管理

- 图标路径使用相对路径，通过 `os.path.join()` 构建
- 确保资源文件存在后再加载，添加异常处理

---

## 二、Git规范

### 2.1 分支管理

> **状态**: 当前仅使用 `main` 分支，其他分支策略为「规划中」，待项目规模扩大后落地

| 分支类型 | 命名规则 | 用途 |
|---------|---------|------|
| 主分支 | `main` | 生产环境代码（当前使用） |
| 开发分支 | `develop` | 开发整合分支（规划中） |
| 功能分支 | `feature/xxx` | 新功能开发（规划中） |
| 修复分支 | `fix/xxx` | Bug修复（规划中） |

### 2.2 提交规范

#### 2.2.1 提交信息格式

```
<类型>(<范围>): <描述>

<详细说明>
```

#### 2.2.2 类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 添加网络校时功能` |
| `fix` | Bug修复 | `fix: 修复音频播放异常` |
| `refactor` | 代码重构 | `refactor: 优化指令列表逻辑` |
| `style` | 样式调整 | `style: 调整按钮间距` |
| `docs` | 文档更新 | `docs: 更新README文档` |
| `chore` | 构建/工具 | `chore: 更新.gitignore` |
| `test` | 测试相关 | `test: 添加单元测试` |

#### 2.2.3 提交示例

```
feat(exam): 添加普通模式考试时间自定义功能

- 支持用户自定义考试开始和结束时间
- 添加时间校验逻辑，确保时间配置有效
- 更新普通模式界面布局，添加时间选择器
```

### 2.3 提交频率

- 每个功能模块完成后进行提交
- Bug修复及时提交
- 避免一次提交包含过多无关改动

---

## 三、开发流程

### 3.1 开发流程

```
1. 创建功能分支 → 2. 开发实现 → 3. 代码审查 → 4. 合并到develop → 5. 测试验证 → 6. 发布到main
```

### 3.2 任务管理

- 使用 TodoList 管理开发任务
- 每个任务完成后更新《说明文档.md》中的进度记录
- 遇到问题及时记录到问题记录表

### 3.3 代码审查

- 重大功能变更需进行代码审查
- 审查内容包括：代码规范、逻辑正确性、性能优化、安全性

---

## 四、测试规范

> **状态**: 当前以手动功能测试为主，单元测试框架为「规划中」

### 4.1 测试类型

| 测试类型 | 说明 | 执行时机 | 状态 |
|---------|------|---------|------|
| 单元测试 | 测试单个函数/方法 | 开发完成后 | 规划中 |
| 集成测试 | 测试模块间交互 | 功能合并前 | 规划中 |
| 功能测试 | 测试完整功能流程 | 版本发布前 | 当前使用 |

### 4.2 测试要求

- 新增功能需进行手动功能验证
- Bug修复后需验证修复效果
- 测试结果记录到《说明文档.md》
- 单元测试框架落地后，新增功能需编写对应的测试用例

---

## 五、发布规范

### 5.1 版本号规则

使用语义化版本控制：`MAJOR.MINOR.PATCH`

| 版本类型 | 说明 |
|---------|------|
| `MAJOR` | 重大功能变更，不兼容旧版本 |
| `MINOR` | 新增功能，兼容旧版本 |
| `PATCH` | Bug修复，兼容旧版本 |

### 5.2 发布流程

1. 更新版本号（`settings.py` 中的 `VERSION`）
2. 更新《说明文档.md》中的版本记录
3. 创建版本标签
4. 推送代码到远程仓库

---

## 六、安全规范

### 6.1 注意事项

- 不存储敏感信息（密码、密钥等）
- 配置文件中的敏感信息使用环境变量
- 网络请求添加超时处理
- 输入数据进行校验，防止注入攻击

### 6.2 日志规范

- 日志中不记录敏感信息
- 日志级别合理使用（DEBUG/INFO/WARNING/ERROR）
- 异常信息记录完整堆栈
